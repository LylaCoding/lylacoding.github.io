function setup() {
    createCanvas(windowWidth, windowHeight);
}

function draw() {
    background(0);
    stroke(255, 151, 33)
    strokeWeight(10)
    line(0, (windowHeight / 2), windowWidth, windowHeight / 2)
}